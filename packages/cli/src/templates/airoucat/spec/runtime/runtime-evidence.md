# Runtime Evidence

Runtime-heavy projects need runtime evidence when behavior changes.

Valid evidence can include logs, reproduction output, simulator output, game or
mod runtime checks, screenshots, recordings, or dedicated debug output.

Without runtime evidence, use:

```text
Implementation complete, runtime unverified.
```

Do not turn passing unit tests into a runtime claim.
